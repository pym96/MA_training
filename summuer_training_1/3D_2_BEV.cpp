#include <iostream>
#include <functional>

class PointType{

    public:
        static PointType* Str;

};

void GenerateBEVImage(PointType::Str cloud){
    
    // Caculating the boundaries of point cloud
    auto minmax_x = std::minimax_element(could->points.begin(), cloud->points.end(),
        [](const PointType)

    )

}
